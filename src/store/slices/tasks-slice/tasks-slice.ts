import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
export const tasksSlice = createSlice({
    name: "tasks",
    initialState,
    reducers: {},
    extraReducers: (builder) => {},
    selectors: {},
});
